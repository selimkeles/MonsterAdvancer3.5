"""
Domain dataclasses for monster advancement.

Distinct from `app.models` (SQLAlchemy ORM, persisted shapes) — this
package holds the in-memory value objects manipulated by the rules and
services layers.
"""
