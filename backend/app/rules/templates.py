"""
SRD — Improving Monsters: Templates  (placeholder — Phase 4)
=============================================================

> Certain creatures are created by adding a template to an existing
> creature. A templated creature can represent a freak of nature, the
> individual creation of a single experimenter, or the first generation
> of offspring from parents of different species.

> Templates often change a creature's type, and may change the creature's
> size. If a template changes the base creature's type, the creature also
> acquires the augmented subtype unless the template description indicates
> otherwise.

This module is a stub. When templates are implemented, each template
should be a small function that takes an `AdvancedMonster` and returns
a modified copy — applying inherited templates before acquired ones,
per the SRD ordering rule.
"""
